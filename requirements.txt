requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
Pillow>=10.0.0
cairosvg>=2.7.0
# apps/tools/check_mobile_viewport.py（mobile-viewport-check.yml から実行）が使う。
# ブラウザ本体は wheel に含まれないので、ワークフロー側で
# `python3 -m playwright install --with-deps chromium` を別途走らせる。
playwright>=1.48.0
